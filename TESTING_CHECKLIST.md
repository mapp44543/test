# ✅ Чек-лист внедрения и валидации

## Шаг 1: Валидация кода ✓

### Компилируется ли проект?
```bash
npm run check
# Должно быть без ошибок TypeScript
```

### Нет ошибок импортов?
```bash
# Проверьте на наличие красных волнистых линий в:
# - src/App.tsx (импорт IconsCacheProvider)
# - src/components/location-marker.tsx (импорт useIconsCache)
```

Если есть ошибки, проверьте:
- Файл `client/src/context/icons-cache.tsx` существует?
- Все импорты используют правильные пути (`@/context/icons-cache`)?

---

## Шаг 2: Запуск приложения

```bash
# В корне проекта
npm run dev

# Приложение должно запуститься без ошибок
# Проверьте консоль браузера на наличие ошибок
```

### Возможные ошибки и решения

| Ошибка | Решение |
|--------|---------|
| `useIconsCache must be used within IconsCacheProvider` | Убедитесь, что `IconsCacheProvider` оборачивает приложение в `App.tsx` |
| `Module not found: 'context/icons-cache'` | Проверьте путь к файлу, должен быть `client/src/context/icons-cache.tsx` |
| `Cannot read property 'commonAreaIcons' of undefined` | `useIconsCache()` не инициализирован правильно |

---

## Шаг 3: Тестирование производительности

### 3.1 Network запросы

1. Откройте DevTools → Network
2. Обновите страницу (Ctrl+R)
3. Дождитесь полной загрузки
4. Фильтр по слову "icon"

**Ожидается:**
- ✅ 3-8 запросов иконок (в зависимости от типов локаций)
- ❌ НЕ 600+ запросов

**Что смотреть:**
```
GET /icons/common%20area/... 
GET /icons/negotiation%20room/...
GET /icons/print/...
GET /icons/Камера/...
GET /icons/ac/...
GET /icons/user/activ/...
GET /icons/user/nonactiv/...
GET /icons/user/repair/...
```

### 3.2 Performance - FPS при панорамировании

1. Откройте DevTools → Performance
2. Нажмите кнопку записи (Record)
3. Начните перемещать карту мышью в течение 5 секунд
4. Остановите запись

**Ожидается:**
- ✅ FPS: 40-60 (зелёная полоса)
- ❌ Частые падения ниже 30 FPS

**Как читать график:**
- Зелёная полоса вверху = хороший FPS
- Красная/оранжевая = плохо, маркеры перерендериваются

### 3.3 React Profiler - проверка перерендеров

1. Откройте DevTools → React DevTools → Profiler
2. Начните запись
3. Панорамируйте карту мышью
4. Остановите запись

**Ожидается:**
- ✅ Компонент `VirtualizedMarkerLayer` не мигает при панорамировании
- ✅ Отдельные маркеры не мигают при панорамировании
- ❌ Маркеры НЕ должны перерендериваться при каждом движении мыши

### 3.4 Memory usage

1. DevTools → Memory → Heap Snapshot
2. Поищите `LocationMarker`
3. Сравните количество экземпляров до и после

**Ожидается:**
- ✅ Меньше экземпляров LocationMarker, чем было при DOM рендеринге
- ✅ Меньше общей памяти

---

## Шаг 4: Функциональное тестирование

### Маркеры работают корректно?

- [ ] Клик на маркер открывает модальное окно
- [ ] Наведение показывает tooltip
- [ ] Иконки загружаются (не пустые)
- [ ] Цвет статуса отображается правильно
- [ ] Drag-drop маркеров работает (админ мод)
- [ ] Поиск локации (Find) работает

### На мобильном устройстве?

- [ ] Touch на маркер открывает модальное окно
- [ ] Pan работает (две пальца)
- [ ] Zoom работает (щипок)

---

## Шаг 5: Тест с разным количеством маркеров

### Создайте тестовые сценарии:

**Сценарий 1: 50 маркеров**
- [ ] FPS ≥ 50
- [ ] Снимаются ли запросы иконок?
- [ ] Плавно ли панорамирование?

**Сценарий 2: 100 маркеров**
- [ ] FPS ≥ 40
- [ ] Есть ли lag?
- [ ] Tooltip появляются вовремя?

**Сценарий 3: 150+ маркеров**
- [ ] FPS ≥ 30
- [ ] Разумное использование памяти?
- [ ] Нужна ли Уровень 2/3 оптимизация?

---

## Шаг 6: Сравнение "До" и "После"

Создайте таблицу результатов:

| Метрика | До оптимизации | После оптимизации | ↓ Улучшение |
|---------|---|---|---|
| API запросы (100 маркеров) | 600 | 15 | 97% ✅ |
| FPS при pan | 25 | 45 | 80% ✅ |
| Time to Interactive (TTI) | 3.5s | 1.8s | 49% ✅ |
| Memory usage | 180MB | 130MB | 28% ✅ |

---

## 🚨 Если что-то не работает

### Проблема: Иконки не загружаются

**Признаки:**
- Маркеры серые, без иконок
- Ошибка в консоли

**Решение:**
1. Проверьте, что иконки существуют в `/public/icons/`
2. Убедитесь, что `useIconsCache()` вызывается внутри провайдера
3. Проверьте Network → есть ли запросы иконок?

### Проблема: React DevTools показывает много перерендеров

**Причины:**
- `scale` и `panPosition` всё ещё в memo сравнении (но мы их исключили)
- Родительский компонент часто перерендеривается

**Решение:**
- Проверьте, что React.memo сравнение правильно в `location-marker.tsx`
- Оберните родительские функции в `useCallback`

### Проблема: Компонент крашится с ошибкой о контексте

**Признаки:**
```
TypeError: useIconsCache must be used within IconsCacheProvider
```

**Решение:**
1. Откройте `App.tsx`
2. Убедитесь, что структура такая:
```tsx
function App() {
  return (
    <QueryClientProvider>
      <TooltipProvider>
        <IconsCacheProvider>  {/* <- ВОТ ЭТО */}
          <Toaster />
          <AppContent />
        </IconsCacheProvider>
      </TooltipProvider>
    </QueryClientProvider>
  );
}
```

---

## 📊 Метрики для мониторинга

### Зелёные светофоры ✅
- FPS ≥ 40 при панорамировании
- Время загрузки иконок < 100ms
- Memory < 150MB для 100 маркеров
- Нет ошибок в консоли

### Жёлтые светофоры 🟡
- FPS 25-40
- Время загрузки 100-300ms
- Memory 150-200MB
- Нужна Уровень 2 оптимизация

### Красные светофоры 🔴
- FPS < 25
- Время загрузки > 300ms
- Memory > 200MB
- Нужна Уровень 3 оптимизация + Canvas

---

## ✅ Финальный чек-лист

Перед тем как считать оптимизацию завершённой:

- [ ] Код компилируется без ошибок (`npm run check`)
- [ ] Приложение запускается без ошибок
- [ ] API запросы иконок сокращены на 90%
- [ ] FPS при панорамировании ≥ 40
- [ ] Маркеры не мигают при панорамировании
- [ ] Все функции работают (клик, drag, поиск)
- [ ] Нет ошибок в DevTools консоли
- [ ] Отличие "до" и "после" зафиксировано в таблице

---

## 📝 Документирование результатов

Создайте файл `OPTIMIZATION_RESULTS.md`:

```markdown
# Результаты оптимизации

## Дата: 25 февраля 2026
## Разработчик: [ваше имя]

### Изменения:
1. ✅ Создан глобальный кэш иконок
2. ✅ Оптимизирована React.memo сравнение
3. ✅ Удалены множественные useCustomIcons запросы

### Результаты:
- API запросы: 600 → 15 (97% сокращение)
- FPS при pan: 25 → 45 FPS (80% улучшение)
- TTI: 3.5s → 1.8s (49% улучшение)
- Memory: 180MB → 130MB (28% сокращение)

### Тестирование:
- Сценарий 50 маркеров: ✅ Пройден
- Сценарий 100 маркеров: ✅ Пройден
- Сценарий 150+ маркеров: ⚠️ Требуется Уровень 2

### Рекомендации:
Если нужна дополнительная оптимизация, смотрите ADVANCED_OPTIMIZATION.md
```

---

**Статус:** 🚀 Готово к тестированию  
**Дата:** 25 февраля 2026  
**Повторить проверку через:** 1 неделю после внедрения
